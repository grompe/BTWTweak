diff --git a/src/minecraft_server/net/minecraft/src/BlockJukeBox.java b/src/minecraft_server/net/minecraft/src/BlockJukeBox.java
index efa2dca..efc38ea 100644
--- a/src/minecraft_server/net/minecraft/src/BlockJukeBox.java
+++ b/src/minecraft_server/net/minecraft/src/BlockJukeBox.java
@@ -121,0 +122,12 @@ public class BlockJukeBox extends BlockContainer
+
+    public boolean OnBlockSawed(World world, int x, int y, int z)
+    {
+        int i;
+        for (i = 0; i < 6; ++i)
+        {
+            FCUtilsItem.EjectSingleItemWithRandomOffset(world, x, y, z, FCBetterThanWolves.fcBlockWoodSidingItemStubID, 0);
+        }
+        FCUtilsItem.EjectSingleItemWithRandomOffset(world, x, y, z, Item.diamond.itemID, 0);
+        world.setBlockToAir(x, y, z);
+        return true;
+    }
