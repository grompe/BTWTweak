diff --git a/src/minecraft/net/minecraft/src/FCBlockGearBox.java b/src/minecraft/net/minecraft/src/FCBlockGearBox.java
index f9a5d12..714b20b 100644
--- a/src/minecraft/net/minecraft/src/FCBlockGearBox.java
+++ b/src/minecraft/net/minecraft/src/FCBlockGearBox.java
@@ -395 +395 @@ public class FCBlockGearBox extends Block implements FCIBlockMechanical, FCIBloc
-        FCUtilsItem.EjectSingleItemWithRandomOffset(var1, var2, var3, var4, Item.redstone.itemID, 0);
+        FCUtilsItem.EjectSingleItemWithRandomOffset(var1, var2, var3, var4, FCBetterThanWolves.fcItemRedstoneLatch.itemID, 0);
@@ -456,0 +457,16 @@ public class FCBlockGearBox extends Block implements FCIBlockMechanical, FCIBloc
+
+    public boolean OnBlockSawed(World world, int x, int y, int z)
+    {
+        int i;
+        for (i = 0; i < 3; ++i)
+        {
+            FCUtilsItem.EjectSingleItemWithRandomOffset(world, x, y, z, FCBetterThanWolves.fcBlockWoodSidingItemStubID, 0);
+        }
+        for (i = 0; i < 3; ++i)
+        {
+            FCUtilsItem.EjectSingleItemWithRandomOffset(world, x, y, z, FCBetterThanWolves.fcGear.itemID, 0);
+        }
+        FCUtilsItem.EjectSingleItemWithRandomOffset(world, x, y, z, FCBetterThanWolves.fcItemRedstoneLatch.itemID, 0);
+        world.setBlockToAir(x, y, z);
+        return true;
+    }
